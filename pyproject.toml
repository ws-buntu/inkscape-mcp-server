[project]
name = "inkscape-mcp-server"
version = "0.1.0"
description = "Self-contained MCP server and agent workflow for Inkscape 1.4.4 design production."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
inkscape-mcp-server = "inkscape_mcp_server.server:main"

[tool.pytest.ini_options]
pythonpath = ["."]
